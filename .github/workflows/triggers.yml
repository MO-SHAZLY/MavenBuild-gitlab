name: trigger action
on: 
  push:
    branches:
      - master
      - 'releases/**'
  pull_request:
      branches:
       - master
       - 'releases/**'
jobs: 
 basic-workflow:
   runs-on: ubuntu-latest 
   steps:  
     - name: print some chars
       run: echo "ITI 44"
  
